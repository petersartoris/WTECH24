<div class="form-group pb-2">
    <label for="{{ $id }}" class="pb-1">{{ $label }}</label>
    <input type="{{ $type }}" class="form-control" id="{{ $id }}" autocomplete="{{ $autocomplete }}">
</div>
